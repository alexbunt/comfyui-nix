# nix-direnv evaluates the flake non-interactively, so Nix cannot prompt to
# accept this repository's binary-cache settings from flake.nix.
export NIX_CONFIG="${NIX_CONFIG-}
accept-flake-config = true"

use flake
